[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Variables de entorno (también puedes configurarlas en el dashboard de Netlify)
# [build.environment]
#   VITE_API_URL = "https://tu-backend.up.railway.app/api"
